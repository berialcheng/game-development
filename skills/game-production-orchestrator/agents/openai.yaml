interface:
  display_name: "Game Production Orchestrator"
  short_description: "Plan game iterations and govern generated assets"
  default_prompt: "Use $game-production-orchestrator to define the next managed-game scope, acceptance criteria, required handoffs, validation level, and stop condition."
