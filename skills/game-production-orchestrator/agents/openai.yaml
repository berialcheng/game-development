interface:
  display_name: "Game Production Orchestrator"
  short_description: "Coordinate only genuinely cross-system game work"
  default_prompt: "Use $game-production-orchestrator only where this outcome needs real coordination; keep the loop lightweight, playable, reversible, and focused on the next useful decision."
