interface:
  display_name: "Generate 2D Sprite"
  short_description: "Pixel sprites, clean HD props, bundles, and transparent GIFs."
  default_prompt: "Use $generate2dsprite to create the smallest useful sprite, prop, FX, portrait, or cutout-character bundle, then run deterministic cleanup and QC without replacing requested art with code-drawn placeholders."
