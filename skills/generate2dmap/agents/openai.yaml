interface:
  display_name: "Generate 2D Map"
  short_description: "Create playable 2D maps, layers, collision, and previews"
  default_prompt: "Use $generate2dmap to create the lightest playable 2D map bundle for this project, including only the needed visual layers, object data, collision, scene hooks, preview, and managed-project handoff."

policy:
  allow_implicit_invocation: true
